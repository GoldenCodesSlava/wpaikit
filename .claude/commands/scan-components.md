---
description: Scan SCSS and Twig files to build a components registry for reuse in code generation
argument-hint: (no arguments — reads main.scss imports and scans views/)
---

Scan project SCSS and Twig files to create `.wpaikit/components-registry.md`.

Follow the prompt at `knowledge/prompts/scan-components.md` exactly.
