---
description: Audit, clean, convert, and connect fonts based on design-system.json
argument-hint: (no arguments — reads .wpaikit/design-system.json and frontend/src/fonts/)
---

Set up project fonts from `.wpaikit/design-system.json` and `frontend/src/fonts/`.

Follow the prompt at `knowledge/prompts/setup-fonts.md` exactly.
